# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty — it contains only a `README.md` (title only, no content) and no source code, build configuration, or tests yet. There is no established language, framework, build system, or architecture to document.

When the user starts adding code to this repository, update this file with:
- The language/framework chosen and how to install dependencies
- Build, lint, and test commands (including how to run a single test)
- High-level architecture once there is more than one file/module to describe

## Language

모든 대화와 커밋 메시지는 한국어로 작성한다.

## 디자인 규칙

- 버튼: 배경색은 연보라(`#bb8cff`), 모서리는 둥글게 `border-radius: 12px`
